# Static reader: no environment variables required.
# Optional local companion reads these from the shell, not from this file:
# SRF_PORT=4317
# SRF_CODEX_BIN=/path/to/codex
# Sign in with `codex login` using ChatGPT. API keys are not supported.
